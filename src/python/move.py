class Hitbox:
	def __init__(self, x, y, radius, damage, hitlag, knockback, knockback_angle):
		self.x = x
		self.y = y
		self.radius = radius
		self.damage = damage
		self.hitlag = hitlag
		self.knockback = knockback
		self.knockback_angle = knockback_angle
